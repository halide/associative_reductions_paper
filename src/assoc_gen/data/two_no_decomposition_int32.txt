Morton: 0, leaves: [2, 4], i: [0, 19999]
{ Add::make(x0, y0), Sub::make(y1, x0) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Add::make(x0, y0) } -> Unknown-associativity
{ Add::make(x0, y0), Add::make(x1, y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Sub::make(x1, y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Add::make(x0, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Add::make(x1, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Min::make(x1, y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(x0, y0), Max::make(x1, y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(x0, y0), Mul::make(x1, y1) } -> Left-associativity with identity: { 0, 1 }
{ Add::make(x0, y0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Sub::make(y1, Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Sub::make(y1, Min::make(Max::make(x0, x1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Sub::make(y1, Max::make(Min::make(x0, y0), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Sub::make(y1, Min::make(Max::make(x0, y0), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Add::make(Max::make(Min::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Add::make(x0, y0), Add::make(Min::make(Max::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Add::make(x0, y0), Mul::make(Add::make(x0, y0), Add::make(x0, y0)) } -> Unknown-associativity
{ Add::make(x0, y0), Sub::make(y1, Max::make(Min::make(x0, y1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y0), Sub::make(y1, Min::make(Max::make(x0, y1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Min::make(x0, y0), Min::make(x0, y0) } -> Unknown-associativity
{ Min::make(x0, y0), Min::make(x1, y0) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Min::make(x0, y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Add::make(x1, y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Min::make(x1, y1) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(x0, y0), Max::make(x1, y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(x0, y0), Mul::make(x1, y1) } -> Left-associativity with identity: { 2147483647, 1 }
{ Min::make(x0, y0), Min::make(Max::make(y0, y1), x0) } -> Unknown-associativity
{ Min::make(x0, y0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Max::make(Min::make(x0, y1), x1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(x0, y0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Min::make(Max::make(x0, x1), y0) } -> Unknown-associativity
{ Min::make(x0, y0), Max::make(Min::make(x1, y0), y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(x0, y0), Max::make(Min::make(Max::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Min::make(x0, y0), Max::make(Min::make(Max::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Min::make(x0, y0), Min::make(Max::make(Min::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Min::make(x0, y0), Min::make(Max::make(Min::make(x1, y0), y1), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(x0, y0), Max::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(x0, y0), Max::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(x0, y0), Max::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(x0, y0), Max::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(x0, y0), Add::make(Max::make(x0, y0), Sub::make(x1, y0)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Min::make(Max::make(x0, x1), Min::make(x1, y0)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(x0, y0), Max::make(Min::make(x0, y1), Min::make(x1, y0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(x0, y0), Max::make(x0, y0) } -> Unknown-associativity
{ Max::make(x0, y0), Max::make(x1, y0) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Max::make(x0, y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Add::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Min::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(x0, y0), Max::make(x1, y1) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(x0, y0), Mul::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 1 }
{ Max::make(x0, y0), Max::make(Min::make(y0, y1), x0) } -> Unknown-associativity
{ Max::make(x0, y0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Min::make(Max::make(x0, y1), x1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(x0, y0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Max::make(Min::make(x0, x1), y0) } -> Unknown-associativity
{ Max::make(x0, y0), Min::make(Max::make(x1, y0), y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(x0, y0), Min::make(Max::make(Min::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Max::make(x0, y0), Min::make(Max::make(Min::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Max::make(x0, y0), Max::make(Min::make(Max::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Max::make(x0, y0), Max::make(Min::make(Max::make(x1, y0), y1), x0) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(x0, y0), Min::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(x0, y0), Min::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(x0, y0), Min::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(x0, y0), Min::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(x0, y0), Add::make(Min::make(x0, y0), Sub::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Max::make(Min::make(x0, x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(x0, y0), Min::make(Max::make(x0, y1), Max::make(x1, y0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Add::make(x1, y0), Add::make(x1, y1) } -> Left-associativity with identity: { 0, 0 }
{ Min::make(x1, y0), Min::make(x1, y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Max::make(x1, y0), Max::make(x1, y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Sub::make(y0, x1), Add::make(x1, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(x0, y1), Add::make(x1, y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(x0, y1), Add::make(x1, y1) } -> Right-associativity with identity: { 0, 0 }
{ Mul::make(x0, y0), Mul::make(x0, y0) } -> Unknown-associativity
{ Mul::make(x0, y0), Add::make(x1, y1) } -> Left-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Mul::make(x1, y0) } -> Right-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Min::make(x1, y1) } -> Left-associativity with identity: { 1, 2147483647 }
{ Mul::make(x0, y0), Max::make(x1, y1) } -> Left-associativity with identity: { 1, -2147483648 }
{ Mul::make(x0, y0), Mul::make(x0, y1) } -> Left-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Mul::make(x1, y1) } -> Left-associativity with identity: { 1, 1 }
{ Mul::make(x0, y0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Add::make(Mul::make(x0, y1), x1) } -> Left-associativity with identity: { 1, 0 }
{ Mul::make(x0, y0), Add::make(Mul::make(x1, y0), y1) } -> Left-associativity with identity: { 1, 0 }
{ Min::make(x0, y1), Min::make(x1, y1) } -> Right-associativity with identity: { 0, 2147483647 }
{ Max::make(x0, y1), Max::make(x1, y1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(x1, y1), Add::make(x1, y1) } -> Unknown-associativity
{ Mul::make(x1, y0), Mul::make(x1, y1) } -> Left-associativity with identity: { 0, 1 }
{ Min::make(x1, y1), Min::make(x1, y1) } -> Unknown-associativity
{ Max::make(x1, y1), Max::make(x1, y1) } -> Unknown-associativity
{ Mul::make(x0, y1), Mul::make(x1, y1) } -> Right-associativity with identity: { 0, 1 }
{ Mul::make(x1, y1), Mul::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Add::make(x1, y1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y0), x0), Min::make(x1, y1) } -> Right-associativity with identity: { 0, 2147483647 }
{ Max::make(Min::make(x0, y0), x0), Max::make(x1, y1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Max::make(Min::make(x0, y0), x0), Mul::make(x1, y1) } -> Right-associativity with identity: { 0, 1 }
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(x0, y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, y1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(x0, y1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y0), x0), Sub::make(Add::make(y1, y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Add::make(Max::make(Min::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Mul::make(Max::make(Min::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y0), x0), Add::make(Min::make(Max::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Mul::make(Min::make(Max::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y0), x0), Add::make(Max::make(Min::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Mul::make(Max::make(Min::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y0), x0), Add::make(Min::make(Max::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Mul::make(Min::make(Max::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(Min::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(Max::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(Min::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(Max::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Min::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), x0), Max::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Add::make(x1, y1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y0), x0), Min::make(x1, y1) } -> Right-associativity with identity: { 0, 2147483647 }
{ Min::make(Max::make(x0, y0), x0), Max::make(x1, y1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Min::make(Max::make(x0, y0), x0), Mul::make(x1, y1) } -> Right-associativity with identity: { 0, 1 }
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(x0, y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, y1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(x0, y1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y0), x0), Sub::make(Add::make(y1, y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Add::make(Max::make(Min::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Mul::make(Max::make(Min::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y0), x0), Add::make(Min::make(Max::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Mul::make(Min::make(Max::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y0), x0), Add::make(Max::make(Min::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Mul::make(Max::make(Min::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y0), x0), Add::make(Min::make(Max::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Mul::make(Min::make(Max::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(Min::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(Max::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(Min::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(Max::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Min::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), x0), Max::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x1, y0), x0), Min::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x1, y0), x0), Max::make(Min::make(x1, y1), x0) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x1, y0), x0), Min::make(Max::make(x0, y1), x1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x1, y0), x0), Max::make(Min::make(Max::make(y0, y1), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x1, y0), x0), Max::make(x1, y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x1, y0), x0), Min::make(Max::make(x1, y1), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x1, y0), x0), Max::make(Min::make(x0, y1), x1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x1, y0), x0), Min::make(Max::make(Min::make(y0, y1), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Add::make(x1, y1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y1), x0), Min::make(x1, y1) } -> Right-associativity with identity: { 0, 2147483647 }
{ Max::make(Min::make(x0, y1), x0), Max::make(x1, y1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Max::make(Min::make(x0, y1), x0), Mul::make(x1, y1) } -> Right-associativity with identity: { 0, 1 }
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(x0, y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, y1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(x0, y1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Max::make(Min::make(x0, y1), x0), Sub::make(Add::make(y1, y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Add::make(Max::make(Min::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Mul::make(Max::make(Min::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y1), x0), Add::make(Min::make(Max::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Mul::make(Min::make(Max::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y1), x0), Add::make(Max::make(Min::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Mul::make(Max::make(Min::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y1), x0), Add::make(Min::make(Max::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Mul::make(Min::make(Max::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(Min::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(Max::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(Min::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(Max::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Min::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), x0), Max::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Add::make(x1, y1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y1), x0), Min::make(x1, y1) } -> Right-associativity with identity: { 0, 2147483647 }
{ Min::make(Max::make(x0, y1), x0), Max::make(x1, y1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Min::make(Max::make(x0, y1), x0), Mul::make(x1, y1) } -> Right-associativity with identity: { 0, 1 }
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(x0, y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, y1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(x0, y1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 0 }
{ Min::make(Max::make(x0, y1), x0), Sub::make(Add::make(y1, y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Add::make(Max::make(Min::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Mul::make(Max::make(Min::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y1), x0), Add::make(Min::make(Max::make(x0, y0), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Mul::make(Min::make(Max::make(x0, y0), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y1), x0), Add::make(Max::make(Min::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Mul::make(Max::make(Min::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y1), x0), Add::make(Min::make(Max::make(x0, y1), x0), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Mul::make(Min::make(Max::make(x0, y1), x0), x0) } -> UNKNOWN associative
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(Min::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(Max::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(Min::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(Max::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Min::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), x0), Max::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(y0, y1), x0), Max::make(Min::make(y0, y1), x1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Min::make(Max::make(y0, y1), x0), Min::make(Max::make(y0, y1), x1) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x0, y0), x1), Max::make(Min::make(x0, y1), x1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x0, y0), x1), Min::make(Max::make(x0, y1), x1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(y0, y1), x1), Max::make(x1, y1) } -> Unknown-associativity
{ Min::make(Max::make(y0, y1), x1), Min::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(x0, x1), y0), Max::make(Min::make(x0, x1), y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Min::make(Max::make(x0, x1), y0), Min::make(Max::make(x0, x1), y1) } -> Left-associativity with identity: { 9856, 2147483647 }
{ Max::make(Min::make(x0, y1), y0), Min::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x0, y1), y0), Max::make(Min::make(x1, y1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x0, y1), y0), Min::make(Max::make(x1, y0), y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(x0, y1), y0), Max::make(x1, y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x0, y1), y0), Min::make(Max::make(x1, y1), y0) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x0, y1), y0), Max::make(Min::make(x1, y0), y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(Min::make(x0, x1), y1), Max::make(x1, y1) } -> Unknown-associativity
{ Min::make(Max::make(x0, x1), y1), Min::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), y1), Max::make(Min::make(x1, y0), y1) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(x0, y0), y1), Min::make(Max::make(x1, y0), y1) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Add::make(Mul::make(x0, y1), y0), Mul::make(x1, y1) } -> Left-associativity with identity: { 0, 1 }
{ Sub::make(Add::make(y1, y0), x1), Max::make(Min::make(x1, y0), x1) } -> Unknown-associativity
{ Sub::make(Add::make(y1, y0), x1), Min::make(Max::make(x1, y0), x1) } -> Unknown-associativity
{ Sub::make(Add::make(y1, y0), x1), Max::make(Min::make(x1, y1), x1) } -> Unknown-associativity
{ Sub::make(Add::make(y1, y0), x1), Min::make(Max::make(x1, y1), x1) } -> Unknown-associativity
{ Mul::make(Add::make(y0, y1), Add::make(x0, x1)), Mul::make(Add::make(y0, y1), Add::make(x0, x1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y0), x1), x0), Max::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x1), x0), Min::make(x1, y1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y1), x1), x0), Max::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y1), x1), x0), Min::make(x1, y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, x0) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(x0, y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(x1, y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(x1, y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(x0, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(x1, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(x1, y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(x1, y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Mul::make(x1, y1) } -> Left-associativity with identity: { 0, 1 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, Min::make(Max::make(x0, x1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, Max::make(Min::make(x0, y0), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, Min::make(Max::make(x0, y0), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(Max::make(Min::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(Min::make(Max::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Mul::make(Add::make(x0, y0), Add::make(x0, y0)) } -> UNKNOWN associative
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, Max::make(Min::make(x0, y1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(y0, y1), y0), x0), Sub::make(y1, Min::make(Max::make(x0, y1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(x0, y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(x1, y0) } -> Right-associativity with identity: { 2147483647, 450946186 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(x0, y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(x1, y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(x1, y1) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(x1, y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Mul::make(x1, y1) } -> Left-associativity with identity: { 2147483647, 1 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(y0, y1), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 2147483647, 4 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 2147483647, 268435455 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x0, y1), x1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 2147483647, 4 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 2147483647, 1 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(x0, x1), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x1, y0), y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(Max::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(Max::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(Min::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(Min::make(x1, y0), y1), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Add::make(Max::make(x0, y0), Sub::make(x1, y0)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Min::make(Max::make(x0, x1), Min::make(x1, y0)) } -> Right-associativity with identity: { 2147483647, -2080374786 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x0), Max::make(Min::make(x0, y1), Min::make(x1, y0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, x0) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(x0, y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(x1, y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(x1, y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(x0, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(x1, y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(x1, y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(x1, y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Mul::make(x1, y1) } -> Left-associativity with identity: { 0, 1 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 16 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { 0, 16 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 16 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { 0, 16 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, Min::make(Max::make(x0, x1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, Max::make(Min::make(x0, y0), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, Min::make(Max::make(x0, y0), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(Max::make(Min::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(Min::make(Max::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Mul::make(Add::make(x0, y0), Add::make(x0, y0)) } -> UNKNOWN associative
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, Max::make(Min::make(x0, y1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(y0, y1), y0), x0), Sub::make(y1, Min::make(Max::make(x0, y1), x0)) } -> Left-associativity with identity: { 0, 0 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(x0, y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(x1, y0) } -> Right-associativity with identity: { -2147483648, -2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(x0, y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(x1, y1) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Mul::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 1 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(y0, y1), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(x1, y0), x1) } -> Right-associativity with identity: { -2147483648, 940570080 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x1, y0), x1) } -> Right-associativity with identity: { -2147483648, 806352848 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x0, y1), x1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(x1, y1), x1) } -> Right-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x1, y1), x1) } -> Right-associativity with identity: { -2147483648, 805830560 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(x0, x1), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x1, y0), y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(Min::make(x1, y0), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(Min::make(x1, y1), x1), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(Max::make(y0, y1), y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(Max::make(x1, y0), y1), x0) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(Min::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Min::make(Max::make(x0, y0), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(Min::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Min::make(Max::make(x0, y1), x0), x1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Add::make(Min::make(x0, y0), Sub::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Max::make(Min::make(x0, x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, -1543241728 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x0), Min::make(Max::make(x0, y1), Max::make(x1, y0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y0), x0), x1), Max::make(x1, y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), x1), Min::make(x1, y1) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), x1), Max::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x0), x1), Min::make(x1, y1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), x1), Max::make(x1, y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), x1), Min::make(x1, y1) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), x1), Max::make(x1, y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), x1), Min::make(x1, y1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), x0), x1), Min::make(Max::make(x0, y1), x1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), x0), x1), Max::make(Min::make(x0, y1), x1) } -> Unknown-associativity
{ Min::make(Max::make(x0, x1), Min::make(x1, y0)), Min::make(x1, y1) } -> Left-associativity with identity: { 2111793022, 2147483647 }
{ Max::make(Min::make(x0, y1), Min::make(x1, y0)), Min::make(x1, y1) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(x0, x1), Max::make(x1, y0)), Max::make(x1, y1) } -> Left-associativity with identity: { -2147483647, -2147483648 }
{ Min::make(Max::make(x0, y1), Max::make(x1, y0)), Max::make(x1, y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
Valid: 197
**************************************************************************

Morton: 1, leaves: [2, 4], i: [20000, 39999]
{ Min::make(Max::make(x0, y0), Min::make(y0, x1)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 1073825792, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y1), y0), x1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, y1), y0), x1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(Min::make(x0, y0), Max::make(y0, x1)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 12582912, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), x1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 473956864, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), x1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -1073741824, -2147483648 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, -391643144 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 574619649 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, -2113667056 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Mul::make(x0, y0), Mul::make(x0, y0)) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 2 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 268435456 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Mul::make(x0, y1), Mul::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 2 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), y0), Sub::make(Add::make(y0, y1), Mul::make(x0, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, x1)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Sub::make(Add::make(y0, y1), Min::make(x0, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Sub::make(Add::make(y0, x1), Max::make(x0, y0)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Max::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 2075655498 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 2080374127 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x1, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Min::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x1, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 536870911 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, -1610612736 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, -2013503596 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, -2013503596 }
{ Min::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, x1)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { -2147483648, 8755969 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Sub::make(Add::make(y0, x1), Min::make(x0, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Sub::make(Add::make(y0, y1), Max::make(x0, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Mul::make(Min::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 1995020950 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 1927579732 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x1, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Add::make(Max::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x0, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Min::make(Max::make(x1, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 2 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 1619525632 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 134217728 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 2048 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, -1476263936 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 32768 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Mul::make(x0, y0), Mul::make(x0, y0)) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, -2147483640 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, -2147483644 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 876613640 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 4096 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 1642074144 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Mul::make(x0, y1), Mul::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 2 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), y0), Sub::make(Add::make(y0, y1), Mul::make(x0, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, x1)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 2147483647, -2147483647 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Sub::make(Add::make(y0, y1), Min::make(x0, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Sub::make(Add::make(y0, x1), Max::make(x0, y0)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Max::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 479635688 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 479635625 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x1, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Min::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x1, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 201326592 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 1058946294 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 1955879680 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 1955961600 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 2147483647, 2130706432 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, x1)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Sub::make(Add::make(y0, x1), Min::make(x0, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Sub::make(Add::make(y0, y1), Max::make(x0, y0)) } -> Left-associativity with identity: { -2147483648, 1073741824 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Mul::make(Min::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 1015764086 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x1, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Add::make(Max::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { -2147483648, 64 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x0, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Min::make(Max::make(x1, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 1073741824 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 1073741824 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 262145 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 262145 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { -2147483648, -1610612736 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 1 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 1 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Mul::make(x0, y0), Mul::make(x0, y0)) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 559290415 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Mul::make(x0, y1), Mul::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), y0), Sub::make(Add::make(y0, y1), Mul::make(x0, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, x1)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { -2147483648, 1 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Sub::make(Add::make(y0, x1), Min::make(x0, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Sub::make(Add::make(y0, y1), Max::make(x0, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Mul::make(Min::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(x0, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 1783238182 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x1, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Add::make(Max::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x0, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Min::make(Max::make(x1, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, -2146959360 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 1 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 2 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, -2139084794 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Mul::make(x0, y0), Mul::make(x0, y0)) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 2 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 2 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Mul::make(x0, y1), Mul::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), y0), Sub::make(Add::make(y0, y1), Mul::make(x0, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, x1)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 2147483647, -2147483633 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Sub::make(Add::make(y0, y1), Min::make(x0, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Sub::make(Add::make(y0, x1), Max::make(x0, y0)) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Mul::make(Max::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(x0, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 1174743096 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 1845328583 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x1, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Add::make(Min::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x0, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Max::make(Min::make(x1, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 1740635932 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 2147483646 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 134283264 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 1413570560 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Mul::make(x0, y0), Mul::make(x0, y0)) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 822345728 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, -1958739968 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Mul::make(x0, y1), Mul::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), y0), Sub::make(Add::make(y0, y1), Mul::make(x0, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, x1)) } -> Right-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 2147483647, 1073745920 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Sub::make(Add::make(y0, y1), Min::make(x0, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Sub::make(Add::make(y0, x1), Max::make(x0, y0)) } -> Right-associativity with identity: { 2147483647, 1443227192 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Max::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 2147483647, 1425532662 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 1944639110 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 352159282 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x1, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Min::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x1, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 835665392 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, x1)) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Sub::make(Add::make(y0, x1), Min::make(x0, y0)) } -> Right-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Sub::make(Add::make(y0, y1), Max::make(x0, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Mul::make(Min::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, -2145386496 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, -1854881406 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x1, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Add::make(Max::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x0, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Min::make(Max::make(x1, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 0, -2147483648 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Add::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 0, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y0) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 4227072 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x1, y0), x1), y0) } -> UNKNOWN associative with right-identity: { 1, -251559936 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x1, y1), x1), y0) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Mul::make(x0, y0), Mul::make(x0, y0)) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, -2146959360 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, x1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 537395272 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y0), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(Min::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Mul::make(x0, y1), Mul::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(Max::make(x0, y1), x0), y1) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), y0), Sub::make(Add::make(y0, y1), Mul::make(x0, y0)) } -> UNKNOWN associative with left-identity: { 1, 0 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, x1)) } -> Right-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(y0, y1), y0), x1) } -> Right-associativity with identity: { 2147483647, 1539087936 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Sub::make(Add::make(y0, y1), Min::make(x0, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Sub::make(Add::make(y0, x1), Max::make(x0, y0)) } -> Right-associativity with identity: { 2147483647, 401991354 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Max::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x1, y0), x1), y0) } -> Right-associativity with identity: { 2147483647, 1425532662 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 1944638740 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { 2147483647, 2004735926 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x1, y0), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Min::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x1, y1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 127 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { 2147483647, 401539648 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Min::make(Max::make(x0, y1), x0), y1) } -> Left-associativity with identity: { 2147483647, 0 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, y0), Max::make(y0, x1)) } -> Right-associativity with identity: { -2147483648, 682407270 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(y0, y1), y0), x1) } -> Right-associativity with identity: { -2147483648, 2147418112 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Sub::make(Add::make(y0, x1), Min::make(x0, y0)) } -> Right-associativity with identity: { -2147483648, -363196894 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Sub::make(Add::make(y0, y1), Max::make(x0, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Mul::make(Min::make(x0, y0), Mul::make(y1, x1)) } -> UNKNOWN associative
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x0), y0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x1, y0), x1), y0) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, -2145386496 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x1, y1), x1), y0) } -> Right-associativity with identity: { -2147483648, -1854881406 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x1, y0), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Add::make(Max::make(x0, y0), Sub::make(y1, y0)) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x0, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Min::make(Max::make(x1, y1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, -2147483647 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, x1), x0), y1) } -> Left-associativity with identity: { -2147483648, 8755969 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Min::make(Max::make(x0, y0), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(x0, y1), x0), y0), Max::make(Max::make(Min::make(x0, y1), x0), y1) } -> Left-associativity with identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(x1, y0), x1), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Min::make(Min::make(Max::make(x1, y0), x1), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 134218496, 2147483647 }
{ Min::make(Max::make(x0, x1), Min::make(x1, y1)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x0, x1), Max::make(x1, y1)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y1), x1), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { -2076180481, 2147483647 }
{ Max::make(Max::make(Min::make(x1, y1), x1), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -1593573376, -2147483648 }
{ Min::make(Min::make(Max::make(x1, y1), x1), y0), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 570425344, 2147483647 }
{ Max::make(Min::make(Max::make(x1, y1), x1), y0), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { -2147483647, -2147483648 }
{ Add::make(Min::make(x1, y1), Sub::make(y0, y1)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 306184192, 2147483647 }
{ Add::make(Max::make(x1, y1), Sub::make(y0, y1)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Left-associativity with identity: { 402653184, -2147483648 }
{ Add::make(Min::make(x0, x1), Min::make(y0, y1)), Add::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Mul::make(Min::make(x0, x1), Min::make(y0, y1)), Mul::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x0, x1), Min::make(y0, y1)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x0, x1), Min::make(y0, y1)), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x0, x1), Min::make(y0, y1)), Max::make(Min::make(x0, x1), Max::make(y1, y0)) } -> Unknown-associativity
{ Min::make(Max::make(x0, y0), Min::make(y0, y1)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x1, y0), Min::make(y0, y1)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Max::make(x0, x1), Max::make(y0, y1)), Add::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Mul::make(Max::make(x0, x1), Max::make(y0, y1)), Mul::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x0, x1), Max::make(y0, y1)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x0, x1), Max::make(y0, y1)), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x0, x1), Max::make(y0, y1)), Min::make(Max::make(x0, x1), Min::make(y1, y0)) } -> Unknown-associativity
{ Max::make(Min::make(x0, y0), Max::make(y0, y1)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x1, y0), Max::make(y0, y1)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x0, x1), Min::make(y1, y0)), Min::make(Max::make(x0, x1), Max::make(y0, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x0, y1), Min::make(y1, y0)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(x1, y1), Min::make(y1, y0)), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x0, x1), Max::make(y1, y0)), Max::make(Min::make(x0, x1), Min::make(y0, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x0, y1), Max::make(y1, y0)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(x1, y1), Max::make(y1, y0)), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), x0), y1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Max::make(Max::make(Min::make(x0, x1), x0), y1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Min::make(Min::make(Max::make(x0, x1), x0), y1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Max::make(Min::make(Max::make(x0, x1), x0), y1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y0), x0), y1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Right-associativity with identity: { -2080374784, 2147483647 }
{ Max::make(Max::make(Min::make(x0, y0), x0), y1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 1100, -2147483648 }
{ Min::make(Min::make(Max::make(x0, y0), x0), y1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 219168768, 2147483647 }
{ Max::make(Min::make(Max::make(x0, y0), x0), y1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Right-associativity with identity: { -2100767342, -2147483648 }
{ Min::make(Max::make(Min::make(x1, y0), x0), y1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x1, y0), x0), y1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(Max::make(Min::make(x0, y1), x0), y1), Max::make(Min::make(x0, x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Min::make(Min::make(Max::make(x0, y1), x0), y1), Min::make(Max::make(x0, x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
Valid: 359
**************************************************************************

Morton: 2, leaves: [5, 7], i: [0, 19999]
Valid: 0
**************************************************************************

Morton: 3, leaves: [5, 7], i: [20000, 39999]
{ Min::make(Sub::make(y0, Min::make(Sub::make(y0, x0), x0)), x0), Sub::make(Add::make(y1, y0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Min::make(Sub::make(y0, Min::make(Sub::make(y0, x0), x0)), x0), Sub::make(Add::make(y1, y0), Min::make(Max::make(x0, x1), x0)) } -> Unknown-associativity
{ Max::make(Sub::make(y0, Max::make(Sub::make(y0, x0), x0)), x0), Sub::make(Add::make(y1, y0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Max::make(Sub::make(y0, Max::make(Sub::make(y0, x0), x0)), x0), Sub::make(Add::make(y1, y0), Min::make(Max::make(x0, x1), x0)) } -> Unknown-associativity
Valid: 0
**************************************************************************

Morton: 4, leaves: [2, 4], i: [40000, 59999]
{ Add::make(Max::make(Min::make(x1, y0), x1), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x1, y0), x1), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x1, y0), x1), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x1, y0), x1), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Mul::make(Max::make(Min::make(x1, y0), x1), y1), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x1, y0), x1), y1), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x1, y0), x1), y1), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x1, y0), x1), y1), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Min::make(Max::make(Min::make(x1, y0), x1), y1), Min::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y0), x1), y1), Min::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y0), x1), y1), Min::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x1, y0), x1), y1), Max::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x1, y0), x1), y1), Max::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x1, y0), x1), y1), Max::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y0), x1), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y0), x1), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y0), x1), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y0), x1), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Mul::make(Min::make(Max::make(x1, y0), x1), y1), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x1, y0), x1), y1), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x1, y0), x1), y1), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x1, y0), x1), y1), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Min::make(Min::make(Max::make(x1, y0), x1), y1), Min::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x1, y0), x1), y1), Min::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x1, y0), x1), y1), Min::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x1), y1), Max::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x1), y1), Max::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x1), y1), Max::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Mul::make(x0, y1), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative with left-identity: { 1, 1 }
{ Add::make(Mul::make(x0, y1), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative with left-identity: { 1, 1 }
{ Add::make(Mul::make(x0, y1), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative with left-identity: { 1, 1 }
{ Add::make(Mul::make(x0, y1), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative with left-identity: { 1, 1 }
{ Add::make(Max::make(Min::make(x1, y1), x1), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x1, y1), x1), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x1, y1), x1), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Max::make(Min::make(x1, y1), x1), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Mul::make(Max::make(Min::make(x1, y1), x1), y1), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x1, y1), x1), y1), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x1, y1), x1), y1), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Mul::make(Max::make(Min::make(x1, y1), x1), y1), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(x1, y1), x1), y1), Max::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x1, y1), x1), y1), Max::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(x1, y1), x1), y1), Max::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Mul::make(x1, y1), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Mul::make(x1, y1), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Mul::make(x1, y1), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Mul::make(x1, y1), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Min::make(Max::make(x1, y1), x1), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y1), x1), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y1), x1), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(x1, y1), x1), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Mul::make(Min::make(Max::make(x1, y1), x1), y1), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x1, y1), x1), y1), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x1, y1), x1), y1), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(x1, y1), x1), y1), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Min::make(Min::make(Max::make(x1, y1), x1), y1), Min::make(Max::make(Min::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x1, y1), x1), y1), Min::make(Min::make(Max::make(x1, y0), x1), y1) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(x1, y1), x1), y1), Min::make(Min::make(Max::make(x1, y1), x1), y1) } -> Unknown-associativity
{ Sub::make(Add::make(y1, y0), Min::make(x1, y1)), Min::make(Max::make(Min::make(x1, y0), x1), y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Sub::make(Add::make(y1, y0), Min::make(x1, y1)), Min::make(Min::make(Max::make(x1, y0), x1), y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Sub::make(Add::make(y1, y0), Min::make(x1, y1)), Min::make(Min::make(Max::make(x1, y1), x1), y1) } -> Left-associativity with identity: { 0, 2147483647 }
{ Add::make(Mul::make(x0, y1), Add::make(y1, y0)), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative with left-identity: { -1, 1 }
{ Add::make(Mul::make(x0, y1), Add::make(y1, y0)), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative with left-identity: { -1, 1 }
{ Add::make(Mul::make(x0, y1), Add::make(y1, y0)), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative with left-identity: { -1, 1 }
{ Add::make(Mul::make(x0, y1), Add::make(y1, y0)), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative with left-identity: { -1, 1 }
{ Sub::make(Add::make(y1, y0), Max::make(x1, y1)), Max::make(Max::make(Min::make(x1, y0), x1), y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Sub::make(Add::make(y1, y0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x1, y0), x1), y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Sub::make(Add::make(y1, y0), Max::make(x1, y1)), Max::make(Max::make(Min::make(x1, y1), x1), y1) } -> Left-associativity with identity: { 0, -2147483648 }
{ Mul::make(Mul::make(x1, y1), Mul::make(x1, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Mul::make(x1, y1), Mul::make(x1, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), y1) } -> UNKNOWN associative
{ Mul::make(Mul::make(x1, y1), Mul::make(x1, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Mul::make(Mul::make(x1, y1), Mul::make(x1, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), y1) } -> UNKNOWN associative
{ Sub::make(Max::make(Min::make(x0, x1), x0), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, x1), x0), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, x1), x0), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, x1), x0), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, x1), x0), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, x1), x0), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, x1), x0), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, x1), x0), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y0), x0), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y0), x0), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y0), x0), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y0), x0), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y0), x0), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y0), x0), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y0), x0), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y0), x0), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y1), x0), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y1), x0), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y1), x0), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Max::make(Min::make(x0, y1), x0), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y1), x0), y1), Add::make(Max::make(Min::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y1), x0), y1), Add::make(Min::make(Max::make(x1, y0), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y1), x0), y1), Add::make(Max::make(Min::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
{ Sub::make(Min::make(Max::make(x0, y1), x0), y1), Add::make(Min::make(Max::make(x1, y1), x1), y1) } -> Right-associativity with identity: { 0, 0 }
Valid: 42
**************************************************************************

Morton: 5, leaves: [2, 4], i: [60000, 79999]
Valid: 0
**************************************************************************

Morton: 6, leaves: [5, 7], i: [40000, 59999]
{ Max::make(Min::make(Max::make(x0, x1), y0), Min::make(x0, x1)), Max::make(Min::make(Max::make(x0, x1), y1), Min::make(x0, x1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, x1), y0), Min::make(x0, x1)), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x0, x1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(Min::make(x0, x1), y0), Max::make(x0, x1)), Max::make(Min::make(Max::make(x0, x1), y1), Min::make(x0, x1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(Min::make(x0, x1), y0), Max::make(x0, x1)), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x0, x1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
Valid: 4
**************************************************************************

Morton: 7, leaves: [5, 7], i: [60000, 79999]
{ Add::make(Sub::make(y0, Max::make(Min::make(x0, y0), x0)), x0), Sub::make(Add::make(Mul::make(y0, y0), x1), Mul::make(x0, x0)) } -> UNKNOWN associative
{ Add::make(Sub::make(y0, Min::make(Max::make(x0, y0), x0)), x0), Sub::make(Add::make(Mul::make(y0, y0), x1), Mul::make(x0, x0)) } -> UNKNOWN associative
Valid: 0
**************************************************************************

Morton: 8, leaves: [2, 4], i: [80000, 99999]
Valid: 0
**************************************************************************

Morton: 9, leaves: [2, 4], i: [100000, 119999]
Valid: 0
**************************************************************************

Morton: 10, leaves: [5, 7], i: [80000, 99999]
Valid: 0
**************************************************************************

Morton: 11, leaves: [5, 7], i: [100000, 119999]
Valid: 0
**************************************************************************

Morton: 12, leaves: [2, 4], i: [120000, 139999]
Valid: 0
**************************************************************************

Morton: 13, leaves: [2, 4], i: [140000, 159999]
Valid: 0
**************************************************************************

Morton: 14, leaves: [5, 7], i: [120000, 139999]
{ Min::make(Max::make(Max::make(Min::make(x0, y0), x0), x1), x0), Add::make(Sub::make(y1, Max::make(Min::make(x0, y1), x0)), x0) } -> Unknown-associativity
{ Min::make(Max::make(Max::make(Min::make(x0, y0), x0), x1), x0), Add::make(Sub::make(y1, Min::make(Max::make(x0, y1), x0)), x0) } -> Unknown-associativity
{ Max::make(Min::make(Min::make(Max::make(x0, y0), x0), x1), x0), Add::make(Sub::make(y1, Max::make(Min::make(x0, y1), x0)), x0) } -> Unknown-associativity
{ Max::make(Min::make(Min::make(Max::make(x0, y0), x0), x1), x0), Add::make(Sub::make(y1, Min::make(Max::make(x0, y1), x0)), x0) } -> Unknown-associativity
Valid: 0
**************************************************************************

Morton: 15, leaves: [5, 7], i: [140000, 159999]
Valid: 0
**************************************************************************

Morton: 16, leaves: [2, 4], i: [160000, 179999]
Valid: 0
**************************************************************************

Morton: 17, leaves: [2, 4], i: [180000, 199999]
Valid: 0
**************************************************************************

Morton: 18, leaves: [5, 7], i: [160000, 179999]
Valid: 0
**************************************************************************

Morton: 19, leaves: [5, 7], i: [180000, 199999]
Valid: 0
**************************************************************************

Morton: 20, leaves: [2, 4], i: [200000, 219999]
Valid: 0
**************************************************************************

Morton: 21, leaves: [2, 4], i: [220000, 239999]
Valid: 0
**************************************************************************

Morton: 22, leaves: [5, 7], i: [200000, 219999]
Valid: 0
**************************************************************************

Morton: 23, leaves: [5, 7], i: [220000, 239999]
Valid: 0
**************************************************************************

Morton: 24, leaves: [2, 4], i: [240000, 259999]
Valid: 0
**************************************************************************

Morton: 25, leaves: [2, 4], i: [260000, 279999]
Valid: 0
**************************************************************************

Morton: 26, leaves: [5, 7], i: [240000, 259999]
{ Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Max::make(Min::make(Sub::make(y0, x0), x0), y0), x0), Max::make(Max::make(Min::make(Sub::make(y0, x0), x0), y0), x0) } -> Unknown-associativity
{ Min::make(Min::make(Max::make(Sub::make(y0, x0), x0), y0), x0), Min::make(Min::make(Max::make(Sub::make(y0, x0), x0), y0), x0) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), x1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Max::make(Min::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)), Min::make(Max::make(Add::make(x0, y0), y1), Add::make(x0, y0)) } -> Unknown-associativity
Valid: 0
**************************************************************************

Morton: 27, leaves: [5, 7], i: [260000, 279999]
{ Min::make(Max::make(Max::make(Min::make(x0, y1), x0), y0), x0), Sub::make(Add::make(y1, y0), Max::make(Min::make(x0, y1), x0)) } -> Unknown-associativity
{ Min::make(Max::make(Max::make(Min::make(x0, y1), x0), y0), x0), Sub::make(Add::make(y1, y0), Min::make(Max::make(x0, y1), x0)) } -> Unknown-associativity
{ Max::make(Min::make(Min::make(Max::make(x0, y1), x0), y0), x0), Sub::make(Add::make(y1, y0), Max::make(Min::make(x0, y1), x0)) } -> Unknown-associativity
{ Max::make(Min::make(Min::make(Max::make(x0, y1), x0), y0), x0), Sub::make(Add::make(y1, y0), Min::make(Max::make(x0, y1), x0)) } -> Unknown-associativity
Valid: 0
**************************************************************************

Morton: 28, leaves: [2, 4], i: [280000, 299999]
Valid: 0
**************************************************************************

Morton: 29, leaves: [2, 4], i: [300000, 319999]
Valid: 0
**************************************************************************

Morton: 30, leaves: [5, 7], i: [280000, 299999]
{ Max::make(Min::make(Max::make(x0, y1), x1), Min::make(x0, y0)), Min::make(Max::make(Min::make(x0, y1), x1), Max::make(x0, y0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y1), x1), Max::make(x0, y0)), Max::make(Min::make(Max::make(x0, y1), x1), Min::make(x0, y0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
Valid: 2
**************************************************************************

Morton: 31, leaves: [5, 7], i: [300000, 319999]
Valid: 0
**************************************************************************

Morton: 32, leaves: [2, 4], i: [320000, 339999]
Valid: 0
**************************************************************************

Morton: 33, leaves: [2, 4], i: [340000, 359999]
Valid: 0
**************************************************************************

Morton: 34, leaves: [5, 7], i: [320000, 339999]
Valid: 0
**************************************************************************

Morton: 35, leaves: [5, 7], i: [340000, 359999]
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -1881669632, 2147483647 }
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 1207959552, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -1049624576, 2147483647 }
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -1074167809, 2147483647 }
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -67108865, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -1912550204, 2147483647 }
{ Max::make(Min::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Min::make(Sub::make(y0, y1), Sub::make(y1, y0)), x0) } -> UNKNOWN associative
{ Max::make(Min::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Max::make(Sub::make(y0, y1), Sub::make(y1, y0)), x0) } -> UNKNOWN associative
{ Max::make(Min::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with right-identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with right-identity: { 1073741824, -2147483648 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Min::make(y0, y1), x0), Max::make(x1, y0)) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 3145728 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x1, y1), Min::make(y1, y0)), x0) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 67215480 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 1084235840 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, -788453322 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Add::make(x0, x1), y0), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Sub::make(x0, x1), y0), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Sub::make(x1, x0), y0), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x1, y0)) } -> UNKNOWN associative with left-identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Max::make(x1, y0), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { -2147483648, 2147483647 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { -2147483648, -2147483648 }
{ Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x1, y0), y1), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative with left-identity: { -2147483648, 2147483647 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, -670302208 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, -1375877262 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, -2147483647 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0) } -> UNKNOWN associative
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Add::make(x0, x1), y0), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Sub::make(x0, x1), y0), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Sub::make(x1, x0), y0), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x0, x1), y1), Min::make(x1, y0)) } -> UNKNOWN associative with left-identity: { 2147483647, -2147483648 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Min::make(Max::make(Min::make(x1, y1), Max::make(y1, y0)), x0) } -> UNKNOWN associative
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x1, y0), x1), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Max::make(Min::make(Max::make(x1, y1), x1), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Min::make(x1, y0), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Min::make(Min::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Min::make(Sub::make(y0, y1), Sub::make(y1, y0)), x0) } -> UNKNOWN associative
{ Min::make(Max::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Max::make(Sub::make(y0, y1), Sub::make(y1, y0)), x0) } -> UNKNOWN associative
{ Min::make(Max::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with right-identity: { -1977614336, 2147483647 }
{ Min::make(Max::make(Max::make(Mul::make(x0, y0), x0), y0), x0), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative
{ Min::make(Max::make(Add::make(x0, x1), y0), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 1895755728, 2147483647 }
{ Min::make(Max::make(Sub::make(x0, x1), y0), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -805304256, 2147483647 }
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2029830144, 2147483647 }
{ Min::make(Max::make(Sub::make(x1, x0), y0), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { -939524096, 2147483647 }
{ Max::make(Min::make(Max::make(x0, x1), y1), Min::make(x1, y0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), y1), Min::make(x1, y0)), Min::make(Max::make(Min::make(x0, y0), y1), Max::make(x1, y0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(Min::make(x0, x1), x0), Max::make(x1, y0)), Min::make(Max::make(Min::make(x0, y1), x1), Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, x1), x0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y0), x0), Max::make(x1, y0)), Min::make(Max::make(Min::make(x0, y1), x1), Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y0), x0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y1), x0), Max::make(x1, y0)), Min::make(Max::make(Min::make(x0, y1), x1), Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y1), x0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), x0), Max::make(x1, y0)), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x1, y0)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), x0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 1276688388, -2147483648 }
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -1392654478, -2147483648 }
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 268435456, -2147483648 }
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -1990985784, -2147483648 }
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2162688, -2147483648 }
{ Max::make(Min::make(Max::make(x1, y1), Min::make(y1, y0)), x0), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 0, -2147483648 }
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 0, -2147483648 }
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 1065418816, -2147483648 }
{ Max::make(Min::make(Add::make(x0, x1), y0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 0, -2147483648 }
{ Max::make(Min::make(Sub::make(x0, x1), y0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 567731840, -2147483648 }
{ Min::make(Max::make(Sub::make(Max::make(y0, y1), x1), x1), x0), Max::make(Min::make(Max::make(x1, y0), x1), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(Max::make(y0, y1), x1), x1), x0), Max::make(Min::make(Max::make(x1, y1), x1), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 58368, -2147483648 }
{ Max::make(Min::make(Sub::make(x1, x0), y0), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 7800080, -2147483648 }
{ Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x1, y0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), y1), Max::make(x1, y0)), Max::make(Min::make(Max::make(x0, y0), y1), Min::make(x1, y0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x1, y1), Max::make(y1, y0)), x0), Max::make(Min::make(Max::make(x0, x1), y1), Min::make(x1, y0)) } -> Right-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y0), x1), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, y1), x1), Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(x0, y0), x1), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Max::make(Min::make(Max::make(x1, y0), x1), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y1), x1), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Add::make(Sub::make(Min::make(x1, y0), Max::make(Min::make(x0, x1), x0)), x0), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 594559232, 2147483647 }
{ Add::make(Sub::make(Max::make(x1, y0), Max::make(Min::make(x0, x1), x0)), x0), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { -217055232, -2147483648 }
{ Max::make(Min::make(Max::make(x1, y1), y0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x1, y0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Add::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Add::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 2147483647 }
{ Add::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, -2147483648 }
{ Mul::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Mul::make(Mul::make(Max::make(x1, y0), y0), Min::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 1, -2147483520 }
{ Mul::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Mul::make(Mul::make(Min::make(x1, y0), y0), Max::make(x1, y0)) } -> UNKNOWN associative with right-identity: { 1, -1809711104 }
{ Mul::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Mul::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Mul::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 1, 2147483647 }
{ Mul::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 1, -2147483648 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Max::make(Min::make(Mul::make(x0, y0), x0), y0), x0) } -> UNKNOWN associative
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(y0, y1), x0), Max::make(x1, y0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 2048 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 8 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 536936448 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 524288 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x1, y1), Min::make(y1, y0)), x0) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 805308349 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 1073741825 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Add::make(x0, x1), y0), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, -2146959360 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, x1), y0), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 1547251728 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x1, x0), y0), Max::make(x1, y0)) } -> Right-associativity with identity: { -2147483648, 0 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, x1), y1), Max::make(x1, y0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y0), Max::make(Min::make(x0, x1), x0)), x0) } -> Right-associativity with identity: { -2147483648, 965788182 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -2147483648, -2147483648 }
{ Max::make(Min::make(Max::make(y0, y1), y0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x1, y0), y1), Max::make(Min::make(x0, x1), x0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0), Add::make(Sub::make(Min::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0), Add::make(Sub::make(Max::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
Valid: 95
**************************************************************************

Morton: 36, leaves: [2, 4], i: [360000, 379999]
Valid: 0
**************************************************************************

Morton: 37, leaves: [2, 4], i: [380000, 399999]
Valid: 0
**************************************************************************

Morton: 38, leaves: [5, 7], i: [360000, 379999]
Valid: 0
**************************************************************************

Morton: 39, leaves: [5, 7], i: [380000, 399999]
Valid: 0
**************************************************************************

Morton: 40, leaves: [2, 4], i: [400000, 419999]
Valid: 0
**************************************************************************

Morton: 41, leaves: [2, 4], i: [420000, 439999]
Valid: 0
**************************************************************************

Morton: 42, leaves: [5, 7], i: [400000, 419999]
Valid: 0
**************************************************************************

Morton: 43, leaves: [5, 7], i: [420000, 439999]
{ Max::make(Min::make(Max::make(x0, y0), x1), Min::make(Max::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y1), x1), Min::make(Max::make(x0, x1), x0)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Max::make(x0, y0), x1), Min::make(Max::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(Min::make(x1, y0), x1), Min::make(Max::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y1), x1), Min::make(Max::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Add::make(Sub::make(Min::make(x1, y0), Min::make(Max::make(x0, x1), x0)), x0), Add::make(Sub::make(Min::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 67108864, 2147483647 }
{ Add::make(Sub::make(Max::make(x1, y0), Min::make(Max::make(x0, x1), x0)), x0), Add::make(Sub::make(Max::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)), Min::make(Max::make(Min::make(Max::make(x0, x1), y0), y1), x0) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y0), Min::make(Max::make(x0, x1), x0)), x0) } -> Right-associativity with identity: { 2147483647, 1 }
{ Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)), Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Min::make(y0, y1), y0), Min::make(Max::make(x0, x1), x0)), Min::make(Max::make(Min::make(x1, y0), y1), Min::make(Max::make(x0, x1), x0)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Add::make(Sub::make(Min::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Add::make(Sub::make(Min::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Add::make(Sub::make(Max::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Add::make(Sub::make(Max::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
Valid: 8
**************************************************************************

Morton: 44, leaves: [2, 4], i: [440000, 459999]
Valid: 0
**************************************************************************

Morton: 45, leaves: [2, 4], i: [460000, 479999]
Valid: 0
**************************************************************************

Morton: 46, leaves: [5, 7], i: [440000, 459999]
Valid: 0
**************************************************************************

Morton: 47, leaves: [5, 7], i: [460000, 479999]
{ Add::make(Sub::make(Mul::make(x1, y0), Max::make(Min::make(x0, x1), x0)), x0), Add::make(Sub::make(Mul::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { -1577056928, 1 }
{ Add::make(Sub::make(Mul::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0), Add::make(Sub::make(Mul::make(x1, y1), Max::make(Min::make(x0, x1), x0)), x0) } -> Unknown-associativity
Valid: 1
**************************************************************************

Morton: 48, leaves: [2, 4], i: [480000, 499999]
Valid: 0
**************************************************************************

Morton: 49, leaves: [2, 4], i: [500000, 519999]
Valid: 0
**************************************************************************

Morton: 50, leaves: [5, 7], i: [480000, 499999]
Valid: 0
**************************************************************************

Morton: 51, leaves: [5, 7], i: [500000, 519999]
Valid: 0
**************************************************************************

Morton: 52, leaves: [2, 4], i: [520000, 539999]
Valid: 0
**************************************************************************

Morton: 53, leaves: [2, 4], i: [540000, 559999]
Valid: 0
**************************************************************************

Morton: 54, leaves: [5, 7], i: [520000, 539999]
{ Max::make(Min::make(Max::make(x0, y0), x1), Min::make(x0, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Max::make(x0, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Min::make(Max::make(Min::make(x0, y0), x1), Max::make(x0, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Min::make(x0, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
Valid: 2
**************************************************************************

Morton: 55, leaves: [5, 7], i: [540000, 559999]
{ Max::make(Min::make(Add::make(x1, y1), y0), Add::make(x1, y1)), Max::make(Min::make(Add::make(x1, y1), y0), Add::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x1, y1), y0), Add::make(x1, y1)), Min::make(Max::make(Add::make(x1, y1), y0), Add::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x1, y1), y0), Add::make(x1, y1)), Max::make(Min::make(Add::make(x1, y1), y0), Add::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x1, y1), y0), Add::make(x1, y1)), Min::make(Max::make(Add::make(x1, y1), y0), Add::make(x1, y1)) } -> Unknown-associativity
Valid: 0
**************************************************************************

Morton: 56, leaves: [2, 4], i: [560000, 579999]
Valid: 0
**************************************************************************

Morton: 57, leaves: [2, 4], i: [580000, 599999]
Valid: 0
**************************************************************************

Morton: 58, leaves: [5, 7], i: [560000, 579999]
{ Add::make(Max::make(Min::make(x0, x1), x0), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Min::make(Max::make(x0, x1), x0), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Max::make(Min::make(x0, y0), x0), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Min::make(Max::make(x0, y0), x0), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Max::make(Min::make(x0, y1), x0), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Add::make(Min::make(Max::make(x0, y1), x0), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Mul::make(Max::make(Min::make(x1, y0), x1), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Mul::make(Min::make(Max::make(x1, y0), x1), Mul::make(x1, y0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 0, 1 }
{ Min::make(Max::make(Min::make(x1, y0), x0), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x1, y0), x0), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Sub::make(Mul::make(x1, y0), Min::make(Max::make(x0, x1), x0)), x0), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 1266679808, 1 }
{ Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Sub::make(Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Sub::make(Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Sub::make(Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0), Sub::make(Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, y0), Min::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y0), Min::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Add::make(x0, y1), Min::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { 2147483647, 1459617792 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, y1), Min::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { 2147483647, -135282688 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y1), Min::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, 2139095039 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Min::make(Add::make(x0, x1), y0), Min::make(x0, y0)) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), Max::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, -1291835393 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(x0, y0), Max::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { 2147483647, 0 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, y0), Max::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, 0 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Add::make(x0, y1), Max::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, 531934 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(x0, y1), Max::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, -1342268416 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, y1), Max::make(y0, x1)), x1) } -> Right-associativity with identity: { 2147483647, -2147483647 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Min::make(x0, y0), Min::make(x0, x1)), x1) } -> Unknown-associativity
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x1, y0), x0), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { 2147483647, 2147483647 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, 2147483647 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { 2147483647, 1 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Add::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative with left-identity: { 2147483647, 0 }
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Sub::make(Add::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Sub::make(Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, y0), Min::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y0), Min::make(y0, x1)), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Add::make(x0, y1), Min::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, 1983512576 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Sub::make(x0, y1), Min::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, -1283057665 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Max::make(Min::make(Max::make(x0, y1), Min::make(y0, x1)), x1) } -> Right-associativity with identity: { -2147483648, -161086976 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), Max::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, 538345520 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(x0, y0), Max::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, 0 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, y0), Max::make(y0, x1)), x1) } -> Right-associativity with identity: { -2147483648, 0 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Add::make(x0, y1), Max::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, 6941184 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(x0, y1), Max::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, -1080033280 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Min::make(x0, y1), Max::make(y0, x1)), x1) } -> UNKNOWN associative with right-identity: { -2147483648, -2147483644 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { -2147483648, 2147483647 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Max::make(x0, y0), Max::make(x0, x1)), x1) } -> Unknown-associativity
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> UNKNOWN associative with left-identity: { -2147483648, 2147483647 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> UNKNOWN associative with left-identity: { -2147483648, 1 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Max::make(Min::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> Unknown-associativity
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Add::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative with left-identity: { -2147483648, -2147483647 }
{ Sub::make(Add::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Sub::make(Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)) } -> UNKNOWN associative
{ Sub::make(Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Min::make(Max::make(x0, y0), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Sub::make(Add::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { -5484048, 2147483647 }
{ Sub::make(Add::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Sub::make(Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Max::make(Min::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
{ Sub::make(Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y0, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Sub::make(Min::make(Max::make(x0, y1), x0), Max::make(Min::make(x0, x1), x0)), Add::make(Sub::make(Mul::make(x1, y1), Min::make(Max::make(x0, x1), x0)), x0) } -> Unknown-associativity
Valid: 43
**************************************************************************

Morton: 59, leaves: [5, 7], i: [580000, 599999]
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(y1, x0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y0), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), y0), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), y0), Max::make(x1, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 1073741824, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { -300941312, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { -1056014388, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> UNKNOWN associative with right-identity: { 0, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Right-associativity with identity: { 0, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> UNKNOWN associative with right-identity: { 0, 2147483647 }
{ Add::make(Min::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> UNKNOWN associative with right-identity: { 0, 2147483647 }
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y0), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, y1), x1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Add::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Min::make(x0, x1), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)), Min::make(Max::make(Sub::make(x1, x0), y1), Min::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x1, y0), x0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(x0, x1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> UNKNOWN associative
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y0), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Min::make(Max::make(Min::make(x0, y1), y0), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), y0), Min::make(x1, y1)) } -> Left-associativity with identity: { -2147483648, 2147483647 }
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { -1943011328, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { -1199521758, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { -1241251840, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 542113793, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> UNKNOWN associative with right-identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Right-associativity with identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> UNKNOWN associative with right-identity: { 0, -2147483648 }
{ Add::make(Max::make(Sub::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> UNKNOWN associative with right-identity: { 0, -2147483648 }
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y0, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(y1, x0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y0), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, y1), x1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Add::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Max::make(x0, x1), y1), Max::make(x1, y1)) } -> Unknown-associativity
{ Add::make(Min::make(Sub::make(y0, y1), y1), Max::make(x1, y1)), Max::make(Min::make(Sub::make(x1, x0), y1), Max::make(x1, y1)) } -> Unknown-associativity
Valid: 24
**************************************************************************

Morton: 60, leaves: [2, 4], i: [600000, 619999]
Valid: 0
**************************************************************************

Morton: 61, leaves: [2, 4], i: [620000, 639999]
Valid: 0
**************************************************************************

Morton: 62, leaves: [5, 7], i: [600000, 619999]
{ Mul::make(Max::make(Min::make(x0, x1), x0), Add::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { -377498066, 377498067 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), Add::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { -1954664198, 1954664199 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), Add::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 1324351489, -1324351488 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), Add::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { -1695355944, 1695355945 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), Add::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, y1), x0), Add::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), Add::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { -15, 16 }
{ Mul::make(Min::make(Max::make(x0, y1), x0), Add::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y1), x1), Add::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 1, 0 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 947912704, 947912703 }
{ Mul::make(Max::make(Min::make(x0, x1), x0), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 1073741825, 1073741824 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { -280755930, -280755931 }
{ Mul::make(Min::make(Max::make(x0, x1), x0), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 309641595, 309641594 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 0, -1 }
{ Mul::make(Max::make(Min::make(x0, y0), x0), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 0, -1 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), Sub::make(y0, y1)), Mul::make(Max::make(Min::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 0, -1 }
{ Mul::make(Min::make(Max::make(x0, y0), x0), Sub::make(y0, y1)), Mul::make(Min::make(Max::make(x1, y0), x1), Sub::make(y0, y1)) } -> UNKNOWN associative with right-identity: { 0, -1 }
Valid: 16
**************************************************************************

Morton: 63, leaves: [5, 7], i: [620000, 639999]
{ Max::make(Min::make(Max::make(x1, y0), x0), Min::make(y0, y1)), Max::make(Min::make(Max::make(x1, y1), x0), Min::make(y0, y1)) } -> Left-associativity with identity: { 2147483647, -2147483648 }
Valid: 1
**************************************************************************
